<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=\, initial-scale=1.0">
    <link rel="stylesheet" href="main.css" >
    <title>Weather App</title>
</head>
<body>
        <div class="container">
            
            
            <div class="search">
            <form action="/" id="form" class="form" method="POST">
                   
                    <input class="inp" name="input" type="text" placeholder="Enter the location" autocomplete="off">
                    <input class="btn" type="submit" value="Go">
                    <!-- <input type="submit" value="submit"> -->
                <!-- <button class="form-submit" type="submit"><input type="submit" value="submit"></button> -->
            </form>
        </div>
                
              

                
            
        
           
                <div class="climate">
                    <img height="150px" src="./assets/<%= data.weather[0].main %>.png" alt="">
                    <h1 class="temp"><%= Math.round(data.main.temp) %>°C  </h1>
                    <p>(<%= data.weather[0].main %>)</p>
                    <h2 class="city"><%= data.name %></h2>
                </div>
                <div class="other">
                    <div class="one">
                        <img height="50px" src="./assets/wind.png" alt="">
                        <div>
                            <p class="wind"><%= data.wind.speed %> km/hr</p>
                            <p>Wind velocity</p>
                        </div>
                    </div>
                    <div class="one">
                        <img height="40px" src="./assets/humidity.png" alt="">
                        <div>
                            <p class="humidity"><%= data.main.humidity %>%</p>
                            <p>Humidity</p>
                        </div>
                    </div>
                </div>
               
        </div>
</body>
</html>